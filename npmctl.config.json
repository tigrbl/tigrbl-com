{
  "schemaVersion": 1,
  "site": "tigrbl.com",
  "repository": "tigrbl/tigrbl-com",
  "orchestrator": "npmctl",
  "dns": {
    "provider": "namecheap",
    "zone": "tigrbl.com",
    "records": [
      {
        "type": "A",
        "host": "@",
        "value": "SELF_HOSTED_DOCKER_IP",
        "ttl": 300
      },
      {
        "type": "CNAME",
        "host": "www",
        "value": "tigrbl.com",
        "ttl": 300
      }
    ]
  },
  "deploy": {
    "target": "self-hosted-docker",
    "service": "tigrbl-com",
    "composeFile": "docker-compose.yaml",
    "proxyManager": {
      "provider": "nginx-proxy-manager",
      "apiEndpoint": "https://nginx.bz23.xyz/"
    }
  },
  "commands": {
    "install": "npm ci",
    "check": "npm run check",
    "build": "npm run build",
    "docker:build": "docker compose build tigrbl-com",
    "dns:plan": "node scripts/namecheap-dns.mjs --plan",
    "deploy:dry-run": "node scripts/deploy.mjs --dry-run",
    "deploy": "docker compose up -d --build --force-recreate --no-deps tigrbl-com"
  }
}
